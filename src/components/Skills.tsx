import { SKILLS } from "../data/resumeData";
export function Skills() { return <section id="skills" className="section"><div className="section-head"><div><div className="eyebrow">03 / Компетенции</div><h2 className="section-title">От стратегии<br/>до результата</h2></div><p className="section-note">Работаю на стыке маркетинга, продукта и коммерции — там, где решения превращаются в рост.</p></div><div className="skills-grid">{SKILLS.map(x=><div className="skill-group" key={x.category}><h3>{x.category}</h3><ul>{x.items.map(i=><li key={i}>{i}</li>)}</ul></div>)}</div></section>; }
