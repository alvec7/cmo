import { CONTACTS } from "../data/resumeData";
export function Contacts() { const rows=[['Email',CONTACTS.email,`mailto:${CONTACTS.email}`],['Telegram',CONTACTS.telegram,CONTACTS.telegramUrl],['Телефон',CONTACTS.phone,`tel:${CONTACTS.phone}`],['LinkedIn','Профиль LinkedIn',CONTACTS.linkedin]]; return <section id="contacts" className="section"><div className="contact-grid"><div className="contact-copy"><div className="eyebrow">05 / Контакты</div><h2 className="section-title">Есть задача<br/>для роста?</h2><p>Напишите пару строк о продукте, стадии и ключевой метрике. Вернусь с ответом и предложу следующий шаг.</p></div><div className="contact-links">{rows.map(([label,value,href])=><a className="contact-link" href={href} key={label} target={label==='Telegram'||label==='LinkedIn'?'_blank':undefined} rel="noreferrer"><span>{label}</span><b>{value}</b><i>↗</i></a>)}</div></div></section>; }
